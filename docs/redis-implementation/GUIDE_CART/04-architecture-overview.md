# Kiến Trúc Tổng Quan - Redis Cart System

## 🏗️ System Architecture

```
┌─────────────────────────────────────────────────────────────────────┐
│                           CLIENT LAYER                               │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  ┌──────────────┐         ┌──────────────┐        ┌──────────────┐│
│  │  Guest User  │         │Authenticated │        │   Browser    ││
│  │              │         │    User      │        │  localStorage││
│  │ X-Guest-ID   │         │Bearer Token  │        │              ││
│  └──────┬───────┘         └──────┬───────┘        └──────┬───────┘│
│         │                        │                       │         │
│         └────────────────────────┼───────────────────────┘         │
│                                  │                                  │
└──────────────────────────────────┼──────────────────────────────────┘
                                   │
                                   ▼
┌─────────────────────────────────────────────────────────────────────┐
│                          BACKEND LAYER                               │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │            optionalAccessTokenValidator Middleware            │  │
│  │  ┌─────────────────────┐      ┌─────────────────────────┐   │  │
│  │  │  Check Auth Header  │─────▶│  Decode JWT if Present  │   │  │
│  │  └─────────────────────┘      └───────────┬─────────────┘   │  │
│  │                                            │                  │  │
│  │  ┌─────────────────────┐      ┌───────────▼─────────────┐   │  │
│  │  │  Extract X-Guest-ID │      │ Set req.decode_auth     │   │  │
│  │  └─────────────────────┘      └─────────────────────────┘   │  │
│  └──────────────────────────────────────────────────────────────┘  │
│                                  │                                  │
│                                  ▼                                  │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │                    Cart Controllers                           │  │
│  │  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐        │  │
│  │  │   Add   │  │   Get   │  │ Update  │  │ Remove  │  etc   │  │
│  │  └────┬────┘  └────┬────┘  └────┬────┘  └────┬────┘        │  │
│  │       └────────────┼────────────┼────────────┘              │  │
│  │                    │            │                            │  │
│  └────────────────────┼────────────┼────────────────────────────┘  │
│                       │            │                               │
└───────────────────────┼────────────┼───────────────────────────────┘
                        │            │
                        ▼            ▼
┌─────────────────────────────────────────────────────────────────────┐
│                         REDIS LAYER (Primary)                        │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │                  Redis (In-Memory Storage)                    │  │
│  │                                                               │  │
│  │  Hash Data Structure:                                        │  │
│  │  ┌──────────────────────────────────────────────────────┐   │  │
│  │  │ cart:guest_uuid-123                                   │   │  │
│  │  │  ├─ product_1: { name, price, discount, qty, ... }   │   │  │
│  │  │  ├─ product_2: { ... }                               │   │  │
│  │  │  └─ product_N: { ... }                               │   │  │
│  │  │                                                        │   │  │
│  │  │ cart:674abc123 (userId)                               │   │  │
│  │  │  ├─ product_X: { ... }                               │   │  │
│  │  │  ├─ product_Y: { ... }                               │   │  │
│  │  │  └─ product_Z: { ... }                               │   │  │
│  │  └──────────────────────────────────────────────────────┘   │  │
│  │                                                               │  │
│  │  TTL: 30 days (2592000 seconds)                             │  │
│  │  Operations: O(1) - HSET, HGET, HDEL, HGETALL              │  │
│  └──────────────────────────────────────────────────────────────┘  │
│                                                                      │
└──────────────────────────┬───────────────────────────────────────────┘
                           │
                           │ Background Sync (Debounced 5s)
                           │ (Authenticated Users Only)
                           ▼
┌─────────────────────────────────────────────────────────────────────┐
│                      MONGODB LAYER (Backup)                          │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │            MongoDB Atlas (Persistent Storage)                 │  │
│  │                                                               │  │
│  │  Cart Collection:                                            │  │
│  │  ┌──────────────────────────────────────────────────────┐   │  │
│  │  │ {                                                     │   │  │
│  │  │   user_id: ObjectId,                                 │   │  │
│  │  │   products: [                                        │   │  │
│  │  │     {                                                │   │  │
│  │  │       product_id: ObjectId,                         │   │  │
│  │  │       quantity: Number,                             │   │  │
│  │  │       added_at: Date,                               │   │  │
│  │  │       price_snapshot: Number,                       │   │  │
│  │  │       discount_snapshot: Number,                    │   │  │
│  │  │       price_after_discount_snapshot: Number,        │   │  │
│  │  │       name_snapshot: String,                        │   │  │
│  │  │       image_snapshot: String                        │   │  │
│  │  │     }                                                │   │  │
│  │  │   ]                                                  │   │  │
│  │  │ }                                                     │   │  │
│  │  └──────────────────────────────────────────────────────┘   │  │
│  │                                                               │  │
│  │  Role: Backup, Fallback, Persistent Storage                 │  │
│  └──────────────────────────────────────────────────────────────┘  │
│                                                                      │
└──────────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Data Flow Diagrams

### **Flow 1: Guest Add to Cart**

```
┌─────────┐
│ Browser │
│ (Guest) │
└────┬────┘
     │ POST /api/collections/cart
     │ Headers: { X-Guest-ID: "uuid-123" }
     │ Body: { product_id, quantity }
     ▼
┌─────────────────────────────────┐
│  optionalAccessTokenValidator   │
│  → No token → Skip              │
└────────────┬────────────────────┘
             ▼
┌─────────────────────────────────┐
│   addProductToCartController    │
│  → Extract X-Guest-ID           │
│  → userId = "guest_uuid-123"    │
└────────────┬────────────────────┘
             ▼
┌─────────────────────────────────┐
│  Query Product from MongoDB     │
│  → Get price, discount, name    │
│  → Calculate priceAfterDiscount │
└────────────┬────────────────────┘
             ▼
┌─────────────────────────────────┐
│     Redis HSET Operation        │
│  Key: cart:guest_uuid-123       │
│  Field: productId               │
│  Value: JSON CartItem           │
│  → EXPIRE 30 days               │
└────────────┬────────────────────┘
             ▼
┌─────────────────────────────────┐
│       Response (1-2ms)          │
│  { cartItem: {...} }            │
└─────────────────────────────────┘
             │
             ▼
       ┌─────────┐
       │ Browser │
       │ Update  │
       │   UI    │
       └─────────┘

Note: Guest không sync MongoDB
```

---

### **Flow 2: Authenticated User Add to Cart**

```
┌─────────┐
│ Browser │
│  (User) │
└────┬────┘
     │ POST /api/collections/cart
     │ Headers: { Authorization: "Bearer token" }
     │ Body: { product_id, quantity }
     ▼
┌─────────────────────────────────┐
│  optionalAccessTokenValidator   │
│  → Verify token                 │
│  → Parse user_id: "674abc123"   │
│  → req.decode_auth = { user_id }│
└────────────┬────────────────────┘
             ▼
┌─────────────────────────────────┐
│   addProductToCartController    │
│  → userId = req.decode_auth.id  │
└────────────┬────────────────────┘
             ▼
┌─────────────────────────────────┐
│  Query Product + Calculate      │
└────────────┬────────────────────┘
             ▼
┌─────────────────────────────────────────────────┐
│           Redis HSET (Primary Write)            │
│  Key: cart:674abc123                            │
│  → Response ngay (1-2ms)                        │
└────────────┬────────────────────────────────────┘
             │
             ├──────────────────────────┐
             ▼                          ▼
┌─────────────────────────┐  ┌─────────────────────────────┐
│    Response to User     │  │ Background Job (Non-Block)  │
│    { cartItem: {...} }  │  │  → scheduleSync(userId, 5s) │
└─────────────────────────┘  └──────────┬──────────────────┘
                                        │ (Wait 5 seconds)
                                        ▼
                             ┌─────────────────────────────┐
                             │   MongoDB Sync (Debounced)  │
                             │  → Read Redis cart          │
                             │  → Update MongoDB collection│
                             │  → Snapshot fields saved    │
                             └─────────────────────────────┘
```

---

### **Flow 3: Get Cart (Authenticated User)**

```
┌─────────┐
│ Browser │
└────┬────┘
     │ GET /api/collections/cart
     │ Headers: { Authorization: "Bearer token" }
     ▼
┌──────────────────────────────┐
│ optionalAccessTokenValidator │
│ → Parse user_id: "674abc123" │
└────────────┬─────────────────┘
             ▼
┌──────────────────────────────┐
│ getCollectionsCartController │
└────────────┬─────────────────┘
             ▼
┌──────────────────────────────────────────┐
│      Redis HGETALL (Try Primary)         │
│      Key: cart:674abc123                 │
└────────────┬─────────────────────────────┘
             │
     ┌───────┴───────┐
     │               │
     ▼               ▼
┌─────────┐    ┌──────────────┐
│ Has Data│    │  Empty Cart  │
└────┬────┘    └──────┬───────┘
     │                │
     │                ▼
     │         ┌────────────────────────────┐
     │         │  Fallback: Load MongoDB    │
     │         │  → Query cart collection   │
     │         │  → Restore to Redis        │
     │         │  → Re-query Redis          │
     │         └──────┬─────────────────────┘
     │                │
     └────────────────┘
                      │
                      ▼
             ┌─────────────────┐
             │ Calculate Total │
             │ count, total    │
             └────────┬────────┘
                      ▼
             ┌─────────────────┐
             │    Response     │
             │ { items, count, │
             │   total }       │
             └─────────────────┘
                      │
                      ▼
                ┌─────────┐
                │ Browser │
                │ Display │
                └─────────┘
```

---

### **Flow 4: Merge Cart on Login**

```
┌────────────────┐
│ Guest Browser  │
│ Has cart items │
└───────┬────────┘
        │ POST /api/users/login
        │ Headers: { X-Guest-ID: "uuid-123" }
        │ Body: { email, password }
        ▼
┌─────────────────────────────────┐
│      loginController            │
│  1. Validate credentials        │
│  2. Generate tokens             │
│  3. Check X-Guest-ID header     │
└────────────┬────────────────────┘
             ▼
┌─────────────────────────────────────────────────────┐
│               mergeCart(guestId, userId)            │
├─────────────────────────────────────────────────────┤
│                                                     │
│  Step 1: Load Both Carts                           │
│  ┌──────────────┐         ┌──────────────┐        │
│  │ Redis:       │         │ Redis:       │        │
│  │ cart:guest_  │         │ cart:674abc  │        │
│  │ uuid-123     │         │              │        │
│  └──────┬───────┘         └──────┬───────┘        │
│         │                        │                 │
│         └───────┬────────────────┘                 │
│                 ▼                                   │
│  Step 2: Merge Strategy                           │
│  ┌─────────────────────────────────────┐          │
│  │ For each guest item:                │          │
│  │  → Product exists in user cart?     │          │
│  │    → YES: Sum quantities            │          │
│  │    → NO: Add new product            │          │
│  └─────────────────────────────────────┘          │
│                 │                                   │
│                 ▼                                   │
│  Step 3: Write Merged Cart                        │
│  ┌─────────────────────────────────────┐          │
│  │ Redis: cart:674abc123               │          │
│  │  → All merged items                 │          │
│  └─────────────────────────────────────┘          │
│                 │                                   │
│                 ▼                                   │
│  Step 4: Delete Guest Cart                        │
│  ┌─────────────────────────────────────┐          │
│  │ Redis: DEL cart:guest_uuid-123      │          │
│  └─────────────────────────────────────┘          │
│                 │                                   │
│                 ▼                                   │
│  Step 5: Schedule MongoDB Sync (5s)               │
│                                                     │
└─────────────────┬───────────────────────────────────┘
                  ▼
┌─────────────────────────────────────────┐
│          Response                       │
│  {                                      │
│    access_token: "...",                 │
│    refresh_token: "...",                │
│    clearGuestId: true  ← Flag           │
│  }                                      │
└─────────────────┬───────────────────────┘
                  ▼
┌─────────────────────────────────────────┐
│       Frontend Handling                 │
│  1. Save tokens                         │
│  2. Check clearGuestId flag             │
│  3. Delete localStorage guest_cart_id   │
│  4. Dispatch "cart-updated" event       │
│  5. Refetch cart with token             │
└─────────────────────────────────────────┘
```

---

### **Flow 5: Payment & Cart Cleanup**

```
┌─────────┐
│  User   │
└────┬────┘
     │ POST /api/payment/create-payment
     │ Headers: { Authorization: "Bearer token" }
     │ Body: { amount, orderInfo }
     ▼
┌──────────────────────────────────┐
│   createPaymentController        │
│  1. Get cart from Redis          │
│  2. Validate cart not empty      │
│  3. Create pending order (MongoDB)│
│  4. Generate VNPay URL           │
│  5. DO NOT clear cart yet        │
└────────────┬─────────────────────┘
             │
             ▼
┌──────────────────────────────────┐
│  Response: { paymentUrl }        │
└────────────┬─────────────────────┘
             │
             ▼
┌──────────────────────────────────┐
│  User redirected to VNPay        │
│  → User pays                     │
│  → VNPay callback                │
└────────────┬─────────────────────┘
             │
             ▼
┌─────────────────────────────────────────────────────┐
│         callBackVnpayController                     │
├─────────────────────────────────────────────────────┤
│  1. Verify signature                                │
│  2. Check response code = "00"                      │
│  3. Find pending order                              │
│  4. Update order status: "success"                  │
│                                                     │
│  5. ✅ Remove purchased items from Redis:          │
│     ┌─────────────────────────────────────┐       │
│     │ For each product in order:          │       │
│     │   → cartRedisService.removeProduct  │       │
│     │      (userId, productId)            │       │
│     └─────────────────────────────────────┘       │
│                                                     │
│  6. Update MongoDB cart:                           │
│     → $pull purchased products                     │
│     → Delete cart if empty                         │
│                                                     │
│  7. Update product stock                           │
│  8. Send email confirmation                        │
└─────────────────────────────────────────────────────┘
             │
             ▼
┌──────────────────────────────────┐
│   Redirect to success page       │
└────────────┬─────────────────────┘
             │
             ▼
┌──────────────────────────────────┐
│  Frontend Refetch Cart           │
│  → Purchased items removed       │
│  → Remaining items kept          │
│  → Cart badge updated            │
└──────────────────────────────────┘
```

---

## 🏛️ Component Responsibilities

### **1. Redis Layer**

**Role:** Primary storage, real-time operations

**Responsibilities:**

- Store cart data in-memory (Hash structure)
- Handle all read/write operations (1-2ms)
- TTL auto-cleanup (30 days)
- Key pattern: `cart:{userId}` or `cart:guest_{uuid}`

**Operations:**

- `HSET`: Add/update product
- `HGET`: Get single product
- `HGETALL`: Get entire cart
- `HDEL`: Remove product
- `DEL`: Clear cart
- `EXPIRE`: Set TTL

**Data Structure:**

```
cart:674abc123 (Hash)
  ├─ product_1: '{"productId":"...","name":"...","price":15000000,...}'
  ├─ product_2: '{"productId":"...","name":"...","price":500000,...}'
  └─ product_N: ...
```

---

### **2. MongoDB Layer**

**Role:** Backup storage, persistent data, fallback

**Responsibilities:**

- Store authenticated user carts only (not guest)
- Provide fallback when Redis empty
- Snapshot pricing data (historical prices)
- Cross-session persistence

**When Used:**

- Background sync every 5s (debounced)
- Fallback when Redis empty
- Order creation (product snapshot)
- Historical data queries

**Schema:**

```json
{
  "user_id": "ObjectId(674abc123)",
  "products": [
    {
      "product_id": "ObjectId(xyz)",
      "quantity": 2,
      "added_at": "2025-01-01T10:00:00Z",
      "price_snapshot": 15000000,
      "discount_snapshot": 10,
      "price_after_discount_snapshot": 13500000,
      "name_snapshot": "Laptop Gaming ASUS",
      "image_snapshot": "https://..."
    }
  ],
  "updated_at": "2025-01-01T10:05:00Z"
}
```

---

### **3. Cart Controllers**

**Role:** Business logic, orchestration

**Responsibilities:**

- Validate requests
- Extract user identity (token or guest ID)
- Call Redis service for operations
- Schedule MongoDB sync (authenticated only)
- Calculate totals
- Handle errors

**Controllers:**

- `addProductToCartController`: Add product to cart
- `getCollectionsCartController`: Get cart with fallback
- `updateQuantityProductInCartController`: Update quantity
- `removeProductToCartController`: Remove single product
- `clearProductInCartController`: Clear entire cart

---

### **4. Middleware**

**Role:** Authentication, authorization, validation

**Key Middleware:**

- `optionalAccessTokenValidator`:
  - Verify token if present
  - Don't require token (allow guest)
  - Set `req.decode_authorization` if authenticated
  - Continue if no token

**Flow:**

```
Request → Middleware → Controller
  ↓
  Check Auth Header?
    → Has token → Verify → Set req.decode_auth
    → No token → Skip → Continue
  ↓
  next()
```

---

### **5. Cart Sync Service**

**Role:** MongoDB synchronization, debouncing, fallback

**Responsibilities:**

- Schedule MongoDB sync (debounced 5s)
- Skip sync for guest users
- Load cart from MongoDB when Redis empty
- Handle sync errors gracefully

**Key Functions:**

- `scheduleSync(userId, delay)`: Debounce sync
- `syncToMongoDB(userId)`: Write Redis → MongoDB
- `loadFromMongoDB(userId)`: Read MongoDB → Redis

**Debouncing Mechanism:**

```
User Action 1 (Add product) → Schedule sync (5s)
User Action 2 (Update qty, 2s later) → Cancel old timer → Schedule sync (5s)
User Action 3 (Add product, 3s later) → Cancel old timer → Schedule sync (5s)
...
No more actions for 5s → Execute sync (1 time only)

Result: Multiple operations → 1 database write
```

---

## 🔐 Security Considerations

### **1. Guest ID Validation**

```
Regex Pattern: /^[a-f0-9]{8}-[a-f0-9]{4}-4[a-f0-9]{3}-[89ab][a-f0-9]{3}-[a-f0-9]{12}$/i

Valid:   "12345678-1234-4123-8123-123456789abc"
Invalid: "invalid-uuid"
Invalid: "'; DROP TABLE carts; --"

Backend:
  → Strict regex validation
  → Reject invalid formats
  → Prevent SQL injection / NoSQL injection
```

---

### **2. Token Blacklist**

```
Redis Blacklist:
  Key: token_blacklist:{token}
  TTL: Same as token expiry

Flow:
  → User logout → Add token to blacklist
  → optionalAccessTokenValidator checks blacklist
  → If blacklisted → Treat as guest
  → Prevent token reuse
```

---

### **3. Rate Limiting**

```
Rate Limit Rules (per IP/user):
  → Add to cart: 10 requests / minute
  → Get cart: 30 requests / minute
  → Update quantity: 20 requests / minute

Implementation:
  → Redis counter: rate_limit:{userId}:{operation}
  → Increment on each request
  → TTL 60 seconds
  → If exceeds limit → Return 429 Too Many Requests
```

---

### **4. Cart Size Limits**

```
Limits:
  → Max products per cart: 50 items
  → Max quantity per product: 99
  → Max total value: 1 billion VND

Validation:
  → Check on add/update operations
  → Return error if exceeds limit
  → Prevent abuse / performance issues
```

---

## 📈 Scalability Strategy

### **1. Redis Cluster (Horizontal Scaling)**

```
Setup:
  → 1 Master node (writes)
  → 2 Replica nodes (reads)
  → Redis Sentinel (auto-failover)

Benefits:
  → Distribute read load (3x capacity)
  → High availability (auto-failover)
  → Zero downtime maintenance
  → 10x+ concurrent users capacity
```

---

### **2. MongoDB Sharding**

```
Shard Key: user_id (hash-based)

Setup:
  → 3 shards
  → Each shard: 1 primary + 2 replicas
  → Config servers: 3 nodes

Benefits:
  → Distribute data across shards
  → Scale write capacity
  → Handle millions of users
```

---

### **3. Caching Strategy**

```
Layer 1: Redis (Hot data)
  → Active carts (last 30 days)
  → 1-2ms access time

Layer 2: MongoDB (Warm data)
  → All authenticated user carts
  → 50-200ms access time
  → Fallback when Redis miss

Layer 3: Cold Storage (Archive)
  → S3/Glacier for old orders
  → Historical analysis
```

---

## ✅ Summary

**Architecture Highlights:**

- 🏗️ **3-Layer Design**: Client → Backend → Storage (Redis + MongoDB)
- ⚡ **Redis Primary**: 1-2ms operations, in-memory speed
- 💾 **MongoDB Backup**: Persistent storage, fallback mechanism
- 🔄 **Dual Flow**: Guest (Redis only) + Authenticated (Redis + MongoDB)
- 🎯 **Smart Middleware**: Optional auth for flexibility
- 🛡️ **Security**: Validation, blacklist, rate limiting
- 📈 **Scalable**: Redis cluster + MongoDB sharding ready
- 🚀 **Performance**: 50-250x faster than MongoDB-only

**Key Design Decisions:**

1. **Redis as Primary**: Speed is priority
2. **MongoDB as Backup**: Reliability for authenticated users
3. **Guest Redis-Only**: Cost optimization, no persistence needed
4. **Debounced Sync**: Reduce database load 75%
5. **Snapshot Fields**: Pricing consistency
6. **Fallback Mechanism**: Zero data loss guarantee
7. **TTL Auto-Cleanup**: No manual maintenance

**Result:** Fast + Reliable + Scalable Cart System
